<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ================= Permissions ================= -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.usb.host" />

    <!-- ================= Application ================= -->
    <application
        android:name="com.za.toptitup.loginlibrary.utils.Topitup"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Top it Up"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:replace="android:allowBackup,android:label,android:supportsRtl">

        <!-- Splash Screen -->
        <activity
            android:name="com.za.toptitup.loginlibrary.activitySplashScreen"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme" />

        <!-- Login -->
        <activity
            android:name="com.za.toptitup.loginlibrary.activity_login"
            android:exported="true"
            android:theme="@style/AppTheme.FullScreen" />

        <!-- Base -->
        <activity
            android:name="com.za.toptitup.loginlibrary.BaseActivity"
            android:theme="@style/AppTheme.FullScreen" />

        <!-- Admin -->
        <activity android:name="com.za.toptitup.loginlibrary.admin.activity_otp" />
        <activity android:name="com.za.toptitup.loginlibrary.admin.activity_activation" />

        <!-- Content Provider -->
        <provider
            android:name="com.za.toptitup.loginlibrary.MyContentProvider"
            android:authorities="com.za.topitup.loginlibrary"
            android:exported="true" />

        <!-- File Provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- Network Receiver -->
        <receiver
            android:name="com.za.toptitup.loginlibrary.utils.NetworkChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
